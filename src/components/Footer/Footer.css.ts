import { style } from "@vanilla-extract/css";

export const footer = style({
    padding: "1rem",
    fontSize: "0.875rem",
    textAlign: "center",
    borderTop: "1px solid currentColor",
    opacity: 0.6,
})